# =====================================================
# PMBOK Project Management - Environment Configuration
# =====================================================

# Database Configuration (MariaDB on QNAP)
DB_HOST=192.168.68.100
DB_PORT=3307
DB_NAME=pmbok_pm
DB_USER=root
DB_PASSWORD=Q2*axylotL

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Secret (for authentication - change this!)
JWT_SECRET=super_secret_random_string_at_least_32_chars_long_12345

# CORS Settings (frontend URL)
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=debug

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# =====================================================
# INSTRUCTIONS:
# 1. Replace 192.168.1.xxx with your QNAP IP address
# 2. Update DB_USER and DB_PASSWORD with your MariaDB credentials
# 3. Change JWT_SECRET to a random string for security
# 4. Keep NODE_ENV as 'development' for now
# =====================================================